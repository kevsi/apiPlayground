// Placeholder — populated by Task 3 (shared types for the AI module).
// Real content lands in the next task and includes:
// - RequestContext, RequestPayload, ResponsePayload, NetworkError
// - Diagnostic, Fix, Rule, Severity, Confidence, DiagnosticSource
export {};
