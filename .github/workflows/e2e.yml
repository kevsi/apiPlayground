name: E2E Tests

on:
  pull_request:
  push:
    branches: [master]

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Set CI secret fallback (forks without secrets configured)
        if: env.AUTH_SIGNING_SECRET == ''
        run: echo "AUTH_SIGNING_SECRET=ci-test-secret-must-be-at-least-32-characters-long" >> $GITHUB_ENV

      - name: Install Playwright browsers
        run: pnpm --dir reqy-web exec playwright install --with-deps chromium

      - name: Build app
        run: pnpm --dir reqy-web build

      - name: Run E2E tests
        run: pnpm --dir reqy-web test:e2e

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: reqy-web/playwright-report/
          retention-days: 7
