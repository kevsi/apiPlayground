/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 16 uses Turbopack by default; path aliases come from tsconfig paths (@/*).
  turbopack: {},
};

module.exports = nextConfig;
